# Monorepo env files are app-local.
#
# Server API: copy apps/server/.env.example to apps/server/.env
# Web app:    copy apps/web/.env.example to apps/web/.env
# Legal app:  use apps/legal-poke/.env when that app is scaffolded.
